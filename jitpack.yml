jdk:
  - openjdk21
install:
  # Build and publish only the library module. The sample ':app' module is an
  # com.android.application that fails ':app:packageRelease' on JitPack and would
  # otherwise fail the whole build, leaving the artifact unpublished.
  - ./gradlew :libadb:publishToMavenLocal
